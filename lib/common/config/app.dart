part of 'config.dart';

class _App {
  // 最大重连次数
  final int maxRetryAttempts = 3;
  // token 保存天数
  final int tokenSaveDays = 5;
  // 底部高度
  final double bottomHeight = 100.0;
  // 浏览器的body最大宽度
  final double webBodyMaxWidth = 480.0;
  // 弹窗标题的高度
  final alertHeaderHeight = 80.0;
  // 弹窗底部的高度
  final alertFooterHeight = 80.0;
}